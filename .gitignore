# ---------- OS / editors ----------
.DS_Store
.idea/
*.swp
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
**/.vscode/*
!**/.vscode/settings.json
!**/.vscode/extensions.json

# ---------- Env ----------
# The root .env (ports for the whole stack) plus frontend/.env and frontend/.env.docker
# are committed defaults (no secrets). Personal overrides go in *.local files - or, for
# the root .env, in an exported shell variable - and the *.local files are ignored:
*.local
.env.local
.env.*.local
token.temp

.~*

# ---------- Python / backend ----------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
build/
dist/
develop-eggs/
downloads/
eggs/
.eggs/
sdist/
wheels/
share/python-wheels/
*.egg-info/
*.egg
.installed.cfg
*.spec
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/
.pytest_cache/
.tox/
.nox/
.hypothesis/
cython_debug/
.coverage
.coverage.*
coverage.xml
htmlcov/
*.cover
*.py,cover
nosetests.xml
.ipynb_checkpoints
profile_default/
ipython_config.py

# Real login credentials. auth.example.yaml is the committed template.
backend/auth.yaml

# Backend runtime artifacts (the app writes these into its CWD)
*.db
db.sqlite3
db.sqlite3-journal
backend/data/
backend/export/
backend/media/

# ---------- Node / frontend ----------
node_modules/
dist/
dist-ssr/
.yarn/
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Playwright end-to-end test artifacts and authentication state
frontend/playwright-report/
frontend/test-results/
frontend/.auth/

# ---------- Release artifacts ----------
/blok.zip

# ---------- Docs ----------
docs/_build/
/site
